{
  "name": "@soulguard/core",
  "version": "0.0.0",
  "bin": {
    "soulguard": "dist/cli.js"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test src/",
    "test:integration": "docker build -f test-integration/Dockerfile -t soulguard-test ../.. && docker run --rm soulguard-test",
    "test:e2e": "docker build -f test-e2e/Dockerfile -t soulguard-e2e ../.. && docker run --rm soulguard-e2e",
    "test:e2e:update": "docker build -f test-e2e/Dockerfile -t soulguard-e2e ../.. && docker run --rm soulguard-e2e bash /app/packages/core/test-e2e/run-tests.sh --update",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.0.0",
    "picocolors": "^1.1.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
