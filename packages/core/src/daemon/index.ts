/**
 * Daemon types — re-exported for public API.
 */
export type {
  ProposalPayload,
  ApprovalResult,
  ProposalOutcome,
  ProposalState,
  Proposal,
  ApprovalChannel,
  CreateChannelFn,
} from "./types.js";
