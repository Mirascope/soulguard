/**
 * Daemon types — re-exported for public API.
 */
export type {
  ProposalFile,
  ProposalPayload,
  PostProposalResult,
  ApprovalResult,
  ProposalOutcome,
  ProposalState,
  Proposal,
  PostResultOutcome,
  ApprovalChannel,
  CreateChannelFn,
} from "./types.js";
