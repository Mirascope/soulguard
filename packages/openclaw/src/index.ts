/**
 * @soulguard/openclaw — OpenClaw framework plugin
 *
 * Provides before_tool_call hooks to intercept writes to vault files,
 * gate cron modifications, and protect extension directories.
 *
 * This package is a stub. Implementation follows after core stabilizes.
 */

export {};
